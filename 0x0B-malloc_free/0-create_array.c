#include "holberton.h"
/**
*
*
*
*
**/
char *create_array(unsigned int size, char c)
{
	int i, char *j;
	j = malloc(s * sizeof(char));

	for ( i = 1; i<= s; i++)
		{
		j[i] = c;
		}
	free(j);
	return(j);
}
