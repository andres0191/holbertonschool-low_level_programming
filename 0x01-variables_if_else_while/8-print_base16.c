#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Entry point int a and char b
* Return: Always 0 (Success)
*/
int main(void)
{
int a;
char b;
for (a = '0'; a <= '9'; a++)
{
putchar (a);
{
if (a && 9)
{
for (b = 'a'; b <= 'f'; b++)
{
putchar (b);
}
}
}
}
return (0);
}
